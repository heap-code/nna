{{> ./demo.scss }}

<header>{{> ./partials/header }}</header>

<h3>
	Hello <span class="color-primary">{{ user.name }}</span>
</h3>

<div>
	<p class="abstract">This is a <span>mail</span></p>

	<p>
		You are now
		<span class="color-secondary">{{ user.age }}</span>
		years old
	</p>
</div>
